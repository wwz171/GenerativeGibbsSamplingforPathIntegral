# Core dependencies
# NOTE: PyTorch and PyTorch Geometric must be installed separately!
# See README.md for installation instructions.
numpy>=1.21.0
tqdm>=4.60.0
numba>=0.56.0
matplotlib>=3.5.0

# Analysis tools (install via: pip install freud-analysis)
freud-analysis>=2.0.0

# For running notebooks
jupyter>=1.0.0
ipykernel>=6.0.0
